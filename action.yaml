name: 'CI Deploy Tools'
description: 'Collection of ci/cd deploy tools'
inputs:
  cmd:
    description: 'Cmd with args'
    required: true
    default: '-h'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.cmd }}